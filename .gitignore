node_modules
dist
dev-dist

# Dados financeiros reais — nunca publicar
src/dadosPlanilha.json

# Arquivos temporários do Supabase CLI (estado local)
supabase/.temp/

# Segredos em variáveis de ambiente — nunca publicar
.env
.env.*
*.env

# Guia local do bot do Telegram — contém segredos (TELEGRAM_SECRET). Nunca publicar.
PASSO-A-PASSO-TELEGRAM.md

# Guia local do backup automático — contém segredo (BACKUP_SECRET). Nunca publicar.
PASSO-A-PASSO-BACKUP.md

# Guia local das notificações push — contém segredo (NOTIFY_SECRET). Nunca publicar.
PASSO-A-PASSO-NOTIFICACOES.md

# Configura��o local do Claude Code
.claude/
