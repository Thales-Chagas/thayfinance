project_id = "pgavedcbyrifjolcuhbr"

# O webhook do WhatsApp é chamado pelo Meta, sem login do Supabase.
# Por isso desligamos a verificação de JWT só nesta função.
[functions.whatsapp]
verify_jwt = false

# O "tradutor de IA" é chamado pelo app. Por enquanto sem exigir login
# (protegido pelo teto de gasto da IA). Quando o login na nuvem entrar,
# voltamos a exigir JWT aqui.
[functions.processar]
verify_jwt = false

# Webhook do bot do Telegram (chamado pelo Telegram, sem login Supabase).
# A própria função valida o header secret do Telegram.
[functions.telegram]
verify_jwt = false
